apiVersion: v1
kind: Service
metadata:
  name: diabetic-readmission-service
  labels:
    app: diabetic-readmission
spec:
  type: NodePort
  selector:
    app: diabetic-readmission
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30800
